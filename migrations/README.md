# Migrations

Better Auths SQL-schema ska **genereras från den exakta låsta dependency-versionen** efter att D1-binding och runtime-konfiguration finns.

Ingen handskriven eller uppskattad auth-databasschema accepteras.

Generera, granska och applicera enligt `docs/deployment.md`. Runtime-migration endpoint ska inte exponeras i produktion.
